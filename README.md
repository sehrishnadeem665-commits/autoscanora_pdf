# AutoScanOra

This repository contains the AutoScanOra web app (Next.js) — a PDF and image tools suite.

Development:

- Install dependencies: `npm install`
- Run dev server: `npm run dev`
- Build: `npm run build`
